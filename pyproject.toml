[project]
name = "nyc-taxi-analytics"
version = "0.1.0"
description = "NYC Taxi analytics portfolio project — DuckDB + Go CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.2",
]

[dependency-groups]
dev = []

[tool.uv.workspace]
members = [
    "dbt",
]
